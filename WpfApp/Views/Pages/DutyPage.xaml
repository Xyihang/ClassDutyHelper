<Page x:Class="ClassDutyHelper.Views.Pages.DutyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="32,32,32,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="值日管理" 
                               FontSize="24" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="管理班级值日生和值日表" 
                               FontSize="14" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource SecondaryButtonStyle}" 
                            Content="重置"
                            Padding="20,12"
                            Command="{Binding ResetDutyCommand}"
                            Margin="0,0,12,0"/>
                    <Button Style="{StaticResource PrimaryButtonStyle}" 
                            Content="添加值日生"
                            Padding="20,12"
                            Command="{Binding AddDutyCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="32,0,32,32">
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="NewDutyName" 
                                 Grid.Column="0"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Text="{Binding NewDutyName}"/>

                        <Button Grid.Column="1"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Content="添加"
                                Padding="24,12"
                                Margin="12,0,0,0"
                                Command="{Binding AddDutyCommand}"/>
                    </Grid>

                    <DataGrid x:Name="DutyDataGrid"
                              Grid.Row="1"
                              Style="{StaticResource ModernDataGridStyle}"
                              ItemsSource="{Binding DutyList}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="序号" 
                                                Binding="{Binding Index}"
                                                Width="80"/>
                            <DataGridTextColumn Header="值日生" 
                                                Binding="{Binding Name}"
                                                Width="*"/>
                            <DataGridTextColumn Header="状态" 
                                                Binding="{Binding StatusText}"
                                                Width="120"/>
                            <DataGridTemplateColumn Header="操作" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                                    Content="编辑"
                                                    Padding="12,8"
                                                    FontSize="13"
                                                    Command="{Binding DataContext.EditDutyCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    CommandParameter="{Binding}"
                                                    Margin="0,0,8,0"/>
                                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                                    Content="删除"
                                                    Padding="12,8"
                                                    FontSize="13"
                                                    Foreground="{StaticResource DangerBrush}"
                                                    Command="{Binding DataContext.DeleteDutyCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>
