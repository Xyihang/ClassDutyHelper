<Window x:Class="ClassDutyHelper.Views.StudentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="学生信息" Height="420" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="20" 
            Background="{StaticResource PrimaryGradientBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" Height="440" VerticalAlignment="Top">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.6" Color="#000000" Direction="270"/>
        </Border.Effect>
        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="学生信息" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,24" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="姓名" 
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           FontSize="13"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="NameTextBox" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,0"/>

                <TextBlock Text="学号" 
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           FontSize="13"
                           Margin="0,0,0,0"/>
                <TextBox x:Name="StudentIdTextBox" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,0"/>

                <TextBlock Text="值日组" 
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           FontSize="13"
                           Margin="0,0,0,0"/>
                <TextBox x:Name="GroupTextBox" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,00"/>

                <CheckBox x:Name="IsEnabledCheckBox" 
                          Content="启用" 
                          IsChecked="True" 
                          Style="{StaticResource ModernCheckBoxStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,20,0,0">
                <Button Content="确定" 
                        Click="OnOkClick" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,12,0"/>
                <Button Content="取消" 
                        Click="OnCancelClick" 
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
